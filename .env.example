# Copia este archivo a .env y ajusta rutas si hace falta.
# GOOGLE_CREDENTIALS_PATH=/ruta/absoluta/a/credentials.json
# GOOGLE_TOKEN_PATH=/ruta/absoluta/a/token.json

# Nunca pongas client_id ni client_secret aquí.
# Esos van solo en credentials.json (ignorado por git).
